# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
*.egg-info/
dist/
build/

# Environment variables
.env

# SSH / private keys — NEVER commit these. Anyone with a private key can log
# into the server it belongs to.
ssh key/
*.key
*.pem
id_rsa*

# Photos + database ARE committed: they are the seeded demo data, so a fresh
# deploy has records and images instead of being empty. (Only sample demo
# images live here — do not commit real casework photos.)
# The vector index is rebuilt automatically on boot, so it is not committed.
qdrant_data/

# Database backups
*.sql.backup
*.dump

# Temp files
temp_*.jpg
temp_*.png

# Binaries
*.msix

# Node
node_modules/

# Sample data directories
sample dead/
sample missing/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
